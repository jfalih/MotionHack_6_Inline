import { Dimensions } from "react-native";

const PRIMARY_COLOR = '#1D81F1';
const DIM_COLOR = '#848484';
const DEVICE_WIDTH = Dimensions.get('window').width;
const DEVICE_HEIGHT = Dimensions.get('window').height;
export {
    PRIMARY_COLOR,
    DIM_COLOR,
    DEVICE_HEIGHT,
    DEVICE_WIDTH
}